spring.application.name=demo

# === TEMPORARY H2 DATABASE (while MySQL is being fixed) ===
# Comment out MySQL config and use H2 for now
# spring.datasource.url=jdbc:mysql://127.0.0.1:3306/tagakturo?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# spring.datasource.username=tagak_user
# spring.datasource.password=tagakturo2025
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Temporary H2 database configuration
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

# JPA / Hibernate for H2
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.defer-datasource-initialization=true
spring.jpa.show-sql=true

# Disable SQL initialization for H2 (schema.sql is MySQL-specific)
<<<<<<< HEAD
spring.sql.init.mode=never
=======
spring.sql.init.mode=always
>>>>>>> V3.23.2026

# Enable H2 console for database management
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Server port (default 8080)
server.port=8080

# JWT settings
jwt.secret=change_this_secret_to_something_long
jwt.expirationMs=86400000

file.upload-dir=uploads
